{
  "name": "demeter-tauri",
  "version": "2.2.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "tauri": "npx tauri",
    "tauri:build": "cd frontend && npm install && npm run build && cd .. && npx tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
