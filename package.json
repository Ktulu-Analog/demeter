{
  "name": "demeter-tauri",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "tauri": "cd frontend && npm install && cd .. && cargo tauri dev",
    "tauri:build": "cd frontend && npm install && npm run build && cd .. && cargo tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
